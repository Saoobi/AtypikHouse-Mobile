import palette from "../../stylesheets/palette";

const styles = {
  container: {
    width: "100%"
  },
  label: {
    fontSize: 18,
    color: palette.black
  },
  input: {
    color: palette.black,
    height: 40,
    borderBottomWidth: 1,
    borderColor: palette.blue
  }
};

export default styles;
